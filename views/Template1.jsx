
import React from 'react';

const Template1 = ({ title, message }) => {
  return (
    <html>
      <head>
        <title>{title}</title>
      </head>
      <body>
        <h1>{message}</h1>
      </body>
    </html>
  );
};

export default Template1;
